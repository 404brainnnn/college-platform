services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: college_platform
    ports:
      - "5432:5432"
    volumes:
      - college-platform-db:/var/lib/postgresql/data

volumes:
  college-platform-db:
